// ShipPath.cpp

#include "ShipPath.h"
#include <queue>
#include <set>
#include <algorithm>

ShipPath::ShipPath(TerrainMap &m, Point start_in, Point finish_in) : Path(m, "ShipPath", start_in, finish_in) {}

bool ShipPath::find() {
    // Implementace metody find
    return false;
}
