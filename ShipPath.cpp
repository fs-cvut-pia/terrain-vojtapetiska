#include "ShipPath.h"

ShipPath::ShipPath(TerrainMap& m, Point start_in, Point finish_in)
    : Path(m, "ShipPath", start_in, finish_in) {}

bool ShipPath::find() {
    // Implementace algoritmu pro nalezení nejkratší cesty pro loď
    // Případně můžete využít metody třídy TerrainMap pro získání výškových informací
    // a implementovat hledání cesty podle specifikací zadání.
    // Upravte podle potřeby.
    return false;
}


